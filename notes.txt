// Read - get products depending on department
const getProducts = (arg, callback) => {
    const queryStr = "SELECT * FROM products WHERE ?";
    connection.query(queryStr, { department_name: arg }, function (err, response) {
        if (err) throw err;
        // callback(); // CALLBACK
        callback(response);
    });
}

// create connection if disconnected. D
const connectDB = () => {
    connection = mysql.createConnection(connection.config);
    connection.connect(function (err) {
        if (err) throw err;
        // run the start function after the connection is made to prompt the user
    });
}



            validate: value => {
                var isValid = value.match(/[0 - 9]/g);
                if (isValid) {
                    return true
                }
                return "Please enter a number"
            }